spring.application.name=pulsetrade-user-service

spring:
    datasource:
        url:jdbc:postgresql://localhost:5432/userdb
        username:postgres
        password:
    jpa:
        hibernate:
            ddl-auto:validate
        show-sql:true
#    flyway:
#        enabled:true
#        locations:classpath:db/migration

server:
    port:8081

jwt:
    secret: pulsetrade-secret
    expiration: 3600000  # 1 hour in ms
